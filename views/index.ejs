<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>White Space</title>
  <link rel="stylesheet" href="/style.css" />
</head>

<body>

  <header>
    <div class="header-inner">
      <h1><a href="/">White Space</a></h1>
      <a href="/new" class="write-btn">Write</a>
    </div>
  </header>


  <main class="home-container">

    <% if (posts.length===0) { %>
      <p class="empty">No posts yet.</p>
      <% } %>

        <% posts.forEach(post=> { %>
          <article class="post">
            <h2>
              <a href="/post/<%= post.id %>">
                <%= post.title %>
              </a>
            </h2>

            <p class="content">
              <%= post.content.substring(0, 150) %>...
            </p>

            <div class="post-actions">
              <a href="/edit/<%= post.id %>" class="btn btn-muted">Edit</a>

              <form action="/delete/<%= post.id %>" method="POST">
                <button type="submit" class="btn btn-danger">Delete</button>
              </form>
            </div>
          </article>
          <% }) %>

  </main>

</body>

</html>